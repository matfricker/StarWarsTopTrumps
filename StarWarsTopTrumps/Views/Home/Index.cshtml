@model StarWarsTopTrumps.Models.GameData

@{
    ViewData["Title"] = "New Game";
}

<div class="alert alert-warning mt-5">
    <p>Choose a player to start the game:</p>
    @Html.ActionLink(@Model.Player1.Name, "start", "home", new { player = 1 }, new { @class = "btn btn-outline-warning" })
    @Html.ActionLink(@Model.Player2.Name, "start", "home", new { player = 2 }, new { @class = "btn btn-outline-warning" })
</div>
